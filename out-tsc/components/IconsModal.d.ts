interface IconsModalProps {
    itemId: string;
    locale: string;
    modelName: string;
    slug: string;
    prodUri: string;
    siteUrl: string;
}
export default function IconsModal({ itemId, locale, slug, prodUri, siteUrl, }: IconsModalProps): import("react/jsx-runtime").JSX.Element;
export {};
